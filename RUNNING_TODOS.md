# Running TODOs

This branch is turning Nudagitty from a DAG drawing surface into a broader practitioner workbench. Keep this file current while the implementation is incomplete.

## Done In This Branch

- [x] Domain examples: added practitioner example models for epidemiology/public health, econometrics/public policy, product analytics/experimentation, ML/data science, operations/reliability/supply chain, and social science/education/psychology while keeping the existing classic examples.
- [x] Example browser: replaced the flat examples dropdown with a two-level domain menu so users can scan by practice area before loading a model.
- [x] Modes: added a basic/domain/pro mode toggle. Basic stays approachable for quick explanation and internet-argument use; domain mode shows recommended tools for the active example domain; pro exposes all modules.
- [x] Design modules: added structured module cards for adjustment, target trial, IV, DiD/event study, RD, synthetic control/CausalImpact, experiments/uplift, mediation, graph refutation, causal discovery, root-cause analysis, and distribution-change attribution.
- [x] Denouement packets: added module-specific output/checklist writing for every built-in example so each example states its intended causal claim, estimand, diagnostics, threats, and report language.
- [x] Completed Simpson example: added a computed final output card with crude recovery association, structural `do(Treatment=1)` versus `do(Treatment=0)` contrast, severity separation fast-read, adjustment set, and paradox verdict.
- [x] Completed ICU example: added the golden "Does the ICU make patients die?" model with Gaussian baseline severity, binary ICU admission, binary mortality, Gaussian triage-score collider, and a computed output card for crude mortality, `do(ICU)` contrast, severity separation, and bad-control warning.
- [x] Completed college example: added the golden "Does college raise earnings?" three-variable model with binary family advantage, binary college attendance, continuous earnings, and a computed output card for raw wage premium versus `do(College)` premium.
- [x] Completed tutoring sign-flip example: added the golden "Does tutoring hurt test scores?" three-variable model with binary academic need, binary tutoring, continuous test score, and a computed output card where the raw association is negative but the `do(Tutoring)` effect is positive.
- [x] Chess model consolidation: reduced the chess narrative to two examples: a paper-shaped nonlinear DGP that conditions on `Elite_sample` but fails to flip the IQ-rating sign, and a compact manually specified compensatory-selection DGP that succeeds. Removed the rich latent scaffolding from the catalog.
- [x] TODO affordances: added visible TODO cards for heavier workflows that are not implemented yet: question-first analysis plan, data-aware DAG/import, and code/export bridge.

## Later

- [x] Brown et al. K562 intervention-mechanics explorer: replace the current faithful full-network viewer as the primary experience. Keep the full graph available as context, but make the main task "pick a perturbation gene and inspect predicted downstream causal mechanics."
  - Product stance: Brown et al. is not primarily a gene-by-gene perturbation showcase. Replogle et al. generated the Perturb-seq intervention data; Brown et al. reanalyze it to estimate an all-pairs ACE matrix `R_hat`, infer a sparse direct network `G_hat`, and study global topology, paths, centrality, and validation. The UI must distinguish "paper-reported aggregate result" from "Nudagitty-derived inspection of the released estimates."
  - Literature context to preserve in copy: true reusable gene-level perturbation examples are still sparse. Key experimental anchors include Perturb-seq / CRISP-seq / CROP-seq, Norman combinatorial CRISPRa, Replogle genome-scale Perturb-seq, and Frangieh Perturb-CITE-seq. Prediction papers such as GEARS and PDGrapher are relevant, but recent benchmarks warn that unseen perturbation prediction often captures systematic perturbed-vs-control variation rather than perturbation-specific biology.
  - Source/provenance labels: every row or card should be tagged as one of `reported in paper`, `released Brown estimate`, or `Nudagitty derived from released estimates`. Do not imply Brown et al. highlighted a specific gene pair unless the paper text does so.
  - Primary screen: a perturbation picker. Selecting `RPS3`, `HSPA9`, `PHB`, `MED10`, `DYNLL1`, etc. shows: guide knockdown/effectiveness metadata, count of significant total effects, count of nonzero direct edges, count of significant direct edges, count of significant mediated/non-edge effects, and a "blast radius" distribution.
  - Intervention scenario: add a slider for `do(gene += delta SD)`. Default can use the observed guide knockdown from Supplementary Data 2 when available, but it must be marked as a derived scenario. Forecast downstream change as `delta * R_hat`, with uncertainty/FDR from the released table.
  - Downstream effects table: for the selected exposure gene, rank outcomes by absolute predicted change or absolute `R_hat`. Columns should include outcome gene, predicted change, `R_hat`, SE, FDR, direct `G_hat`, path length, shortest-path effect, effect explained, and sign agreement between direct and total effect.
  - Mechanics cards: automatically surface examples of (1) direct edge and total effect agree, (2) significant total effect with no direct sparse edge, (3) direct and total signs disagree, (4) shortest path explains little of the total effect, and (5) shortest path exceeds 100%, meaning other paths cancel part of that path's effect.
  - Graph surface: default to selected-gene subgraphs, not the 788-node hairball. Provide toggles for `total effects`, `direct G_hat edges`, and `shortest-path explanation for selected pair`. The full graph should be a secondary "all released edges" view.
  - Calibration anchors from the paper: show 788 genes, 10,423 nonzero `G_hat` edges, 131,943 FDR 5% ACEs including self-effects, 47.5% connected ordered pairs, median connected path length about 2.67, median FDR-significant path length about 2.46, median shortest-path effect explained 11.14%, and 5,448 FDR-significant pairs above 100% shortest-path effect explained.
  - Biological interpretation: include centrality/essentiality/heredity panels only as secondary context. Useful contrasts include "high outdegree is not the same as high intervention blast radius" and "centrality can mark constrained/essential genes, but this is network biology rather than validated therapeutic targeting."
  - Data model: added a compact per-exposure index of released `R_hat` rows, especially top FDR-significant total effects and examples where `G_hat = 0`.
  - Validation: added a browser test that selects known genes, verifies paper anchor counts, verifies mediated-only/sign-disagreement examples, and verifies direct-edge/full-graph/total-effect mode switching.
  - Out of scope for this pass: claiming experimental validation of specific gene-pair stories, predicting unseen perturbations beyond Brown's released estimates, or turning the inferred cyclic network into a clean small DAG.
- [x] Ota et al. gene-program-trait reconstruction: add a complicated paper-derived pro example for "Causal modelling of gene effects from regulators to programs to traits" with biology and evidence layers separated. The graph includes CRISPRi regulator perturbation, cNMF-style programs, MCH/RDW/IRF outcomes, LoF gamma, GWAS/trans-eQTL evidence, S_het adjustment, and denouement copy warning that association evidence is not a direct gene-to-trait mechanism.
- [ ] Brown et al. K562 follow-up polish: add a clearer biological interpretation panel comparing high outdegree, intervention blast radius, essentiality, and heritability; improve label collision in selected-gene subgraphs; add a downloadable table for the selected perturbation's stored total effects.
- [ ] Data-aware DAG: import CSV, map columns to graph nodes, inspect missingness/types, run balance and overlap checks, and test graph implications against data.
- [ ] Synthetic datasets: add bundled or generated datasets aligned to each domain example so practitioners can move from diagram to diagnostics without hunting for data.
- [ ] Code/export bridge: generate reproducible R/Python/Stata skeletons for the selected design module and export a collaborator-facing report.
- [ ] Analysis plan mode: add a deliberately more demanding workflow for population, unit, time zero, treatment strategies, estimand, contrast, data source, and design assumptions.
- [ ] Alternative graph compare: support competing DAG versions and show which identification claims survive across alternatives.
- [ ] Assumption ledger: attach evidence, risk, measurement source, and comments to nodes/edges, then include them in exported reports.
